class Solution {
public:
    string toLowerCase(string s)
    {
        for (auto& elem : s)
        {
            if (elem >= 'A' && elem <= 'Z')
            {
                elem += 32;
            }
        }
        
        return s;
    }
};
